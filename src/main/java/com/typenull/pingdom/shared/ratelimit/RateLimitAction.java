package com.typenull.pingdom.shared.ratelimit;

public enum RateLimitAction {
    LOGIN,
    TOKEN_REFRESH,
    EMAIL_RESEND,
    POST_REPORT,
    MAP_IMAGE_LIKE,
    RECOMMENDATION_CLICK,
    IMAGE_UPLOAD
}
